# Before starting work, get the latest changes
git pull origin main

# After making changes to your files
git add .
git commit -m "Describe what you changed"
git push

# See what files have changed
git status

# See actual changes in files
git diff


# setup one liner (todo junto)
git init && git add . && git commit -m "Initial commit" && git branch -M main && git remote add 
origin https://github.com/your-username/repo-name.git && git push -u origin main